
  # landing page seedo

  This is a code bundle for landing page seedo. The original project is available at https://www.figma.com/design/Vnemr2gaAFyytdMfE4Xmcl/landing-page-seedo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  